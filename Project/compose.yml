services:
  backend:
    build:
      context: ./Backend
    container_name: createyourself-backend
    init: true
    command: ["node", "app.js"]
    env_file:
      - ./Backend/.env
    ports:
      - "3000:3000"
    restart: unless-stopped